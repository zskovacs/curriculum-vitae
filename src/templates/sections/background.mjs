import { escapeHtml } from "../html.mjs";
export function renderBackground(content) { return `<div class="editorial-section background-grid"><section id="education" aria-labelledby="education-title"><p class="section-number" aria-hidden="true">06</p><h2 id="education-title">${escapeHtml(content.educationLabel)}</h2><h3>${escapeHtml(content.education.degree)}</h3><p>${escapeHtml(content.education.institution)}</p><p>${escapeHtml(content.education.period)} · ${escapeHtml(content.education.specialization)}</p><p>${escapeHtml(content.education.thesis)}</p></section><section id="languages" aria-labelledby="languages-title"><p class="section-number" aria-hidden="true">07</p><h2 id="languages-title">${escapeHtml(content.languagesLabel)}</h2><ul class="language-list">${content.languages.map(item => `<li><strong>${escapeHtml(item.language)}</strong> — ${escapeHtml(item.level)}</li>`).join("")}</ul><p class="additional-technologies">${content.additionalTechnologies.map(item => escapeHtml(item)).join(" · ")}</p></section></div>`; }
