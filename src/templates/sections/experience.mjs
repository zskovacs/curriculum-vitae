import { escapeHtml } from "../html.mjs";
function renderPosition(position, technologiesLabel, featured) { return `<article class="experience-entry${featured ? " experience-entry-featured" : ""}"><p class="experience-period">${escapeHtml(position.period)}</p><div><h3>${escapeHtml(position.role)}</h3><p class="experience-company">${escapeHtml(position.company)} <span>· ${escapeHtml(position.industry)}</span></p><ul>${position.bullets.map(bullet => `<li>${escapeHtml(bullet)}</li>`).join("")}</ul>${position.technologies ? `<p class="technologies"><strong>${escapeHtml(technologiesLabel)}:</strong> ${escapeHtml(position.technologies)}</p>` : ""}</div></article>`; }
export function renderExperience(content) { return `<section id="experience" class="editorial-section" aria-labelledby="experience-title"><p class="section-number" aria-hidden="true">05</p><div class="section-content"><h2 id="experience-title">${escapeHtml(content.label)}</h2><div class="experience-list">${content.positions.map((position, index) => renderPosition(position, content.technologiesLabel, index < 2)).join("")}</div></div></section>`; }
