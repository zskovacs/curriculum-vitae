import { escapeHtml } from "../html.mjs";
export function renderProject(content) { const project = content.project; return `<section id="fithub" class="editorial-section project" data-surface="project" aria-labelledby="project-title"><p class="section-number" aria-hidden="true">04</p><article class="section-content"><p class="eyebrow">${escapeHtml(content.label)}</p><h2 id="project-title"><a href="${escapeHtml(project.url)}">${escapeHtml(project.title)}</a></h2><p class="project-period">${escapeHtml(project.period)}</p><p class="lede">${escapeHtml(project.description)}</p><dl class="project-details"><div><dt>${escapeHtml(project.labels.stack)}</dt><dd>${escapeHtml(project.stack)}</dd></div><div><dt>${escapeHtml(project.labels.infrastructure)}</dt><dd>${escapeHtml(project.infrastructure)}</dd></div><div><dt>${escapeHtml(project.labels.observability)}</dt><dd>${escapeHtml(project.observability)}</dd></div><div><dt>${escapeHtml(project.labels.workflow)}</dt><dd>${escapeHtml(project.workflow)}</dd></div></dl></article></section>`; }
