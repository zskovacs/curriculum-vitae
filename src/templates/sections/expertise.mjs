import { escapeHtml } from "../html.mjs";
export function renderExpertise(content) { return `<section id="expertise" class="editorial-section" aria-labelledby="expertise-title"><p class="section-number" aria-hidden="true">02</p><div class="section-content"><h2 id="expertise-title">${escapeHtml(content.label)}</h2><div class="expertise-groups">${content.groups.map((group, index) => `<section class="expertise-group" aria-labelledby="${escapeHtml(group.id)}"><p class="item-number" aria-hidden="true">0${index + 1}</p><h3 id="${escapeHtml(group.id)}">${escapeHtml(group.title)}</h3><ul>${group.items.map(item => `<li>${escapeHtml(item)}</li>`).join("")}</ul></section>`).join("")}</div></div></section>`; }
