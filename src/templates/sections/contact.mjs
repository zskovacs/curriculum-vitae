import { escapeHtml } from "../html.mjs";
export function renderContact(content) { const phone = content.contact.phone.replace(/[^+\d]/g, ""); const displayedPhone = escapeHtml(content.contact.phone).replace(/ /g, "&nbsp;"); return `<footer id="contact" class="contact-footer" aria-labelledby="contact-title"><p class="section-number" aria-hidden="true">08</p><div><h2 id="contact-title">${escapeHtml(content.label)}</h2><ul><li><a href="tel:${escapeHtml(phone)}">${displayedPhone}</a></li><li><a href="mailto:${escapeHtml(content.contact.email)}">${escapeHtml(content.contact.email)}</a></li><li><a href="${escapeHtml(content.contact.linkedIn)}">LinkedIn</a></li><li><a href="${escapeHtml(content.contact.gitHub)}">GitHub</a></li></ul></div></footer>`; }
